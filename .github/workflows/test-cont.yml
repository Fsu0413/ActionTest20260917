name: test-cont

on:
  workflow_dispatch:

jobs:
  test1:
    runs-on: ubuntu-latest
    environment: 1
    steps:
      - name: 1
        run: |
          if ! [ "${{ github.secrets.TEST_SECRET_1 }}" = "" ]; then
            echo "Using Test Secret 1"
          elif ! [ "${{ github.secrets.TEST_SECRET_2 }}" = "" ]; then
            echo "Using Test Secret 2"
          else
            echo "No Test Secret injected"
          fi

          exit 0
